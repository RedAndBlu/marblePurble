import React from 'react';
import ReactDOM from 'react-dom';
import '../scss/style.scss';

const testElement = <div>It is working</div>;

function champ() {
  const p = 10;
}

ReactDOM.render(testElement, document.getElementById('root'));
